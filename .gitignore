# Raw datasets: too large for git (8+ GB) and carry their own licensing
# terms (DARK FACE, LFW) that don't permit redistribution. See README for
# download instructions.
datasets/

# Derived YOLO-format copy of DARK FACE images/labels (regenerated by
# finetune/prepare_yolo_face_dataset.py) -- same size/licensing reasons.
finetune/yolo_face/images/
finetune/yolo_face/labels/
finetune/yolo_face/*.cache

# Python cache
__pycache__/
*.pyc

# Jupyter autosave checkpoints (created locally when opening notebooks
# in JupyterLab; not meaningful content)
.ipynb_checkpoints/

# Stray empty artifact directory from an early timing test
/runs/

# OS cruft
.DS_Store
Thumbs.db
